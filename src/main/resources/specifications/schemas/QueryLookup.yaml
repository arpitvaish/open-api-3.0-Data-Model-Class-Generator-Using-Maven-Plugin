components:
  schemas:
    QueryLookup:
      type: object
      required:
        - outputAttributes
      properties:
        outputAttributes:
          type: string
          format: string
        metaData:
          type: string